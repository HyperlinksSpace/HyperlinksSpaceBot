# Copy to .env and fill in. Used by bot (npm run bot:local) and optionally by Expo.
# Do not commit .env.

# Telegram bot (required for local bot; optional for Expo app)
BOT_TOKEN=

# Optional: production domain so webhook is stable (e.g. https://hsbexpo.vercel.app). If unset, VERCEL_URL is used.
# SELF_URL=https://your-app.vercel.app
