# Bot service URL (auth + /api/chat). Copy to .env and set your values.
# Local: bot usually on 8080; prod: set to your bot's public URL (e.g. https://your-bot.railway.app).
BOT_API_URL=http://127.0.0.1:8080
INNER_CALLS_KEY=local-dev-inner-calls-key
# Optional alias for API key:
# BOT_API_KEY=local-dev-inner-calls-key

# Optional: force frontend to use proxy instead of direct BOT_API_URL
# AI_PROXY_URL=http://127.0.0.1:3000/api/ai
